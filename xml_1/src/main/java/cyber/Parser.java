package cyber;

import javax.xml.stream.XMLStreamReader;
import java.util.List;

public class Parser {
    public static List<PersonInformation> parse(XMLStreamReader reader) throws Exception{
        while(reader.hasNext()){

        }
    }

}
